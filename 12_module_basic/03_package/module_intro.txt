"""
    第三方库中的模块
    @如果想要使用第三方库中的模块，先使用工具pip3下载安装第三方库，然后通过import语句进行导入
    @安装路径/root/anaconda3/lib/python3.8/site-packages/
    @PyPI简介
        PyPI的全程是：Python Package Index
        可以上传下载自己的库
        https://pypi.org
    @pip3
    @python3 -m pip
    @which pip3
    @pip3 list
    @pip3 install xxx=y.y
    @pip3 install
    @pip3 install --upgrade xx
    @pip3 uninstall xxx
    @pip3 uninstall xxx==y.y
    @pip3 <命令> --help
"""

"""
    通过setuptools来设置模块
    setup.py
    #python3 setup.py sdist
    #python3 install dist/my_python_lib-0.1.tar.gz 
"""
