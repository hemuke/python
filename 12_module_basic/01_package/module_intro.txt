"""
模块的概述

    @可以在模块中定义变量、函数和类。定义在模块中的变量、函数和类统称为模块的属性
    @模块就是Python的一个.py文件

模块的作用
    @在一个模块中可以导入另一个模块，从而重用另一个模块重定义的属性
    @不同的模块可以存在相同名称的属性

组织和管理模块
    @在目录下添加模块__init__.py之后就变成包了
    @__init__.py的作用是初始化其所在的包，不需要初始化，内容可以为空
    @包 和 子包
"""
