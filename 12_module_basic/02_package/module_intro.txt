"""
    @ 标准模块的安装路径/root/anaconda3/lib/python3.8
    @ 导入整个模块 import [包名.]模块名 如果被导入的模块在一个包结构中，
      那么必须要通过其所有的父包导航到该模块：顶层父包名.子包名.....子包名
      [包名.]模块名.属性名访问
      import xml.dom.minidom
      print(xml.dom.minidom.StringTypes)
    @ 当输入"[包名.][子包名.]模块名.属性名" 
      属性名里面:黄色v(variable) 
                 蓝色f(function)
                 红色c(class)
"""

"""
     @ 导入模块中的属性
       导入模块中某个属性的语法格式为:from [包名.]模块名 import 属性名。
       导入模块中的属性之后，就可以直接访问模块中的属性了，而无需添加前缀'[包名.]模块名'
"""


"""
     @ 导入模块中的多个属性
       导入模块中某个属性的语法格式为:from [包名.]模块名 import 属性名A,属性名B。
       导入模块中的属性之后，就可以直接访问模块中的属性了，而无需添加前缀'[包名.]模块名'
"""
